# PASTEC Chrome Extension Environment Variables
# Copy this file to .env and fill in your values

# Backend API URL
API_URL=http://localhost:9000/api

# Default credentials for development (NEVER commit real credentials)
# In production, these should be entered by users in the extension options page
DEFAULT_USERNAME=
DEFAULT_PASSWORD=
